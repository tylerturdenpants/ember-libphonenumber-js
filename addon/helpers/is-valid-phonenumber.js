import { helper } from '@ember/component/helper';
import { isValidNumber } from 'libphonenumber-js'

export function isValidPhoneNumber(params/*, hash*/) {
  return params;
}

export default helper(isValidPhoneNumber);
